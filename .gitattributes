# Windows 上 core.autocrlf 会把 .sh 存成 CRLF，导致 Linux 执行时报
# bad interpreter: /bin/bash^M —— 所有 shell 脚本强制 LF。
*.sh text eol=lf

# 换行符无关紧要的常见文本/数据文件，统一 LF 避免 diff 噪音
*.sql text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.conf text eol=lf
*.py text eol=lf
