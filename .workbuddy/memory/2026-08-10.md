# 2026-08-10

## DM 手册解析：替换付费阿里云 OCR 为本地免费方案

用户痛点：扫描件/图片 PDF 依赖阿里云 OCR（太贵）；旧版二进制 `.doc` 被代码拒绝。

改动（默认行为不变，配置开关启用）：
- `app/core/config.py`：新增 `OCR_ENGINE`（默认 aliyun）与 `LIBREOFFICE_PATH`；校验仅允许 aliyun/rapid/paddle/tesseract。
- `app/services/ocr.py`：`ocr_image` 改为引擎分发；新增本地免费后端 rapid(RapidOCR-ONNX)/paddle(PaddleOCR)/tesseract，懒加载+实例缓存；保留 `_ocr_image_aliyun`。复用既有 `render_page_png` + `blocks_from_ocr`。
- `app/tasks/dm_ingest.py`：`ensure_local_file` 收尾时把 `.doc` 用 LibreOffice 无头转 `.docx`（缓存为 `<hash>.doc.converted.docx`）；`prepare_document` 按真实落盘后缀判断格式；`extract_shard` OCR 分支仅在 aliyun 时构建阿里云客户端，本地引擎直连。
- `app/services/dm_service.py`：`_resolve_ref` 不再拒绝 `.doc`。
- `requirements.txt`：补充可选本地 OCR 依赖注释。

推荐默认本地引擎：rapidocr-onnxruntime（中文准、轻量、纯离线）。

用户约束：可本地自主操作、不上线；介意 OCR 成本。后续若做文档解析相关，优先本地免费方案而非云付费 API。

## import-status 整体完成节点分析

用户问：import-status 有没有"整体完成"节点，怕写入向量库后前端不知道解析是否结束。
结论：有，`overall_status === "ready"`（三阶段 phases 中 key="ready" 的 status="done"）即终态，代表"可问答/解析结束"。
链路：worker `finalize`(dm_ingest.py:814) 先写回文档 total_chunks 再标 job=completed；get_status 中 indexed=bool(doc且total_chunks>0)(dm_service.py:309)；indexed 为真→overall="ready"。
隐藏坑：整体终态**只认 indexed，不认 job 完成**。若某次解析 completed 却产出 0 chunk（空文档/全被丢弃），indexed 恒 False，overall 落到末尾 else:"pending"，与"未开始"无区别，前端卡死看不到结束（parse_status 已 done 但无对应出口）。
建议：把 job 处于终态 completed/skipped 也作为整体完成信号，或新增 `parsed` 中间态区分"已解析完成待激活"，避免 0-chunk 完成被吞进 pending。

## 已实施 import-status 整体完成节点修复

用户确认要改。采用"新增 parsed 中间态"方案：
- `app/services/dm_service.py` `get_import_status` 整体判定：新增 `elif parse_status == "done": overall = "ready" if status.indexed else "parsed"`。0-chunk 退化完成（job=completed 但 indexed=False）现在返回 `parsed`，不再落到 pending 与"未开始"混淆。
- `app/schemas/dm_guide.py` `ImportStatus.overall_status` 注释补充 `parsed 解析完成(待激活索引/暂不可问答)`。
- 新增 `tests/test_import_status.py`（可 `python tests/test_import_status.py` 直接跑，也兼容 pytest），覆盖 0-chunk→parsed、正常→ready、skipped→parsed、parsing/failed/no_guide/cancelled 等；`.venv` 下 7/7 通过。
前端据此：`overall_status in ("parsed","ready")` 即代表"解析结束"。

## 修复 stage_detail 口径问题（卡 x块/Y问答不动）

现象：embedding 阶段 stageDetail 很快到"x 块/Y 问答"后一直不动。根因：`embed_and_store` 每批上报 `已向量化 {本批len} 块 / {本批len} 问答`（单批口径），多批并行互相覆盖且永远是小数字；最慢的 `generate_qa`(T3) 完全不上报文案。
改动（app/tasks/dm_ingest.py）：
- `embed_and_store` 新增参数 `total_chunks`（chunk_and_dedup 派发时传 len(kept)）；stage_detail 改累计口径：`已向量化 {count_chunks实际行数}/{total_chunks} 块 / {count_qa} 问答`——用库内实际行数（与 finalize 同真相源），重试不虚高。
- `generate_qa` 生成后追加一次 bump_job 上报 `问答对生成中：已累计 N 条`（累计值取自 bump_job RPC `returning *` 返回的整行，避免读-改-写竞态）。
注意：embedded_chunks/embedded_qa/total_qa 计数器仍是 SQL += 原子自增（防并发丢更新），重试时可能虚高；展示口径已全部改为实际行数。
新增 tests/test_ingest_stage_detail.py（3 用例：累计口径/无 total 降级/QA 累计上报），连同 test_import_status.py 全部通过（.venv 直跑，兼容 pytest）。
