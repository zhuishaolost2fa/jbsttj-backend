# 2026-08-27

## 故事还原 + 划线评论全栈能力落地（Task 1-4 全部完成）

- **Task 4 `app/tasks/dm_ingest.py` 流水线改造完成**：
  - T3 `generate_qa`：同时调 `generate_stories(chunks, script_title=)`，payload 增加 `"stories"` 键
  - T4 `embed_and_store`：stories 按 `title + "\n" + content` 向量化后 `insert_stories`；进度计数沿用绝对值回写（embedded_stories）
  - `finalize`：`count_stories` 回写 total_stories；完成后调 `store.reanchor_highlights(document_id)`（force 重跑场景把 orphaned 划线重新锚定）
  - `prepare_document` 幂等跳过分支也回写 `total_stories`
- **Task 2 API 层完成**（`/api/v1/dm-guide` 前缀，ask_router）：
  - `GET /stories`（code/title + storyType 过滤，带 publicHighlights 计数，公开可读）
  - `GET /stories/{story_id}`（详情 + meta + 公开划线时间线分页）
  - `GET /highlights`（共读时间线=按剧本公开划线 / `mine=true` 我的划线含 private）
  - `POST /highlights`（Web Annotation 锚点：quote/startOffset/endOffset/prefix/suffix/visibility，需登录，重复 409）
  - `PATCH /highlights/{id}`（改 comment/visibility，仅本人，403）
  - `DELETE /highlights/{id}`（软删 deleted_at，仅本人）
- `sql/dm_story.sql` **末尾追加了 `list_dm_stories` RPC 函数**（服务层 list_stories 依赖它）——用户执行 SQL 在此之前，**需在 Supabase 单独执行第 441 行起的函数定义**，否则 GET /stories 报 404/函数不存在
- schemas：`StoryItem/StoryDetail/HighlightRecord/StoryListResult/HighlightListResult/CreateHighlightRequest/UpdateHighlightRequest`；`JobProgress`/`DMGuideStatus` 透出 `totalStories/embeddedStories`
- dm_store：新增 `get_stories_by_ids`（时间线列表附故事标题用）
- 测试：修复 test_ingest_stage_detail / test_dm_qa_created_by 的 FakeStore（补 count_stories），43 个 DM 测试全绿

## 遗留
- 单测新增（划线所有权/重复/列表范围校验）读到 test_dm_questions.py 风格后被用户打断，尚未写
- H5 前端对接（ImportDmGuide 之外的共读页）未开始
