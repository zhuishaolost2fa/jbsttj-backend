# 2026-08-21

## 剧本「求解析」功能（script_requests）收尾
- 完成全链路：`sql/script_requests.sql`（建表 + (user_id, match_key) 唯一约束 + 状态 CHECK + updated_at 触发 + RLS）→ `app/schemas/script_request.py`（camelCase 出参）→ `ScriptRequestRepository` → `ScriptRequestService` → `app/api/v1/script_requests.py`（前缀 /scripts/requests，注册在 scripts.router 之前避免路径冲突）。
- 核心语义：库内剧本 match_key=script_id、库外=normalize_title_key(title)；同用户同剧本仅一条诉求；重复发起幂等返回（already_exists=true）、已取消可复活；已完成拦截 409 script_already_parsed；取消是软取消；已解析状态靠读取时惰性同步（对照 script_dm_documents is_active 且 total_chunks>0），两段式：直接 script_id 批量完成 + 库外诉求按归一化标题/别名回填。
- 修复 2 个测试失败，根因与改动：
  1. `_insert_request` 关联到库内剧本时未回填真实标题 → 现在 `script_title` 以 script.title 为准（用户手写变体标题不再入库）。
  2. `ScriptRequestRepository.get_scripts` 跨表查 scripts 属分层错误，且缺 aliases 列导致库外诉求别名匹配静默失效 → 新增 `ScriptRepository.get_scripts`（选 id,code,title,aliases,cover_url，默认排除软删除），服务层 3 处调用点（leaderboard / _backfill_unlinked / _to_items）改走 self.scripts，请求仓储删除 get_scripts。
- 验收：tests/test_script_request_service.py 16/16 通过；pytest 全量 70 passed；OpenAPI 确认 4 个路由注册正确（POST /scripts/requests、GET /me、GET /leaderboard、DELETE /{request_id}）。
