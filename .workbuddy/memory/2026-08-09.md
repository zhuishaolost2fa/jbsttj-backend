# 2026-08-09 工作记录

## DM RAG 流水线真实联调（病娇男孩的精分日记 PDF，20 页扫描件）

- Supabase 已执行 `sql/dm_rag.sql`，四张表 `script_dm_jobs/documents/chunks/qa` 均返回 200 存在。
- OSS 上传正常；剧本行 `dm-bingjiao-nanhai`（id=25be02dc-...）已建好，PDF objectKey=`dm-guides/dm-bingjiao-nanhai.pdf`。
- 全流程已跑到「分块/去重」阶段，卡在最后一步：**硅基流动 API Key 无效**。
  - 用户给的 key `sk-yrwalcchifmwbhzflvpuyekugrzclbgvsnjdahebtsifvwly`（51 字符）打 `https://api.siliconflow.cn/v1/embeddings` 返回 **401 `Token is invalid.`**（code 30014）。
  - 这是密钥本身无效/失效/复制错误，非代码问题。需用户从 https://cloud.siliconflow.cn/ 账户→API密钥 重新复制一个有效 key 替换 `.env` 第 82 行。
  - QA 生成与向量化 embedding 都依赖此 key（bge-large-zh-v1.5 走硅基流动），key 无效则 chunks 无法入库。

## OCR 兜底 bug 修复（已验证）

- 原 `extract_shard` 把「逐页 OCR 的 for 循环」整体包在一个 try 里 → 任一页超时即中止全部后续页。实测 20 页只拿到 9 页。
- 修复：`app/tasks/dm_ingest.py` 把 try/except 移入**每页内部**（单页失败仅跳过）；`app/services/ocr.py` 客户端加 `read_timeout=60000 / connect_timeout=10000`（大图扫描页易超默认 10s）。
- 验证：`_t_ocr_all.py` 跑全本 → **20/20 页识别，13933 字，0 空页**（此前 9 页）。

## 待办（用户侧）

- 提供有效 SiliconFlow API Key 后即可重跑 `scripts/drive_dm_ingest.py` 跑通整条（OCR→分块→LLM问答→向量化入库→可检索）。
- 本地仍为 `CELERY_EAGER=true`（同步跑，不要用 400 页全本，会超时）；上线 Railway 前需删/改此行。
- Redis 未装：去重自动降级为进程内（warning，不影响功能，仅跨分片重复可能漏网）。

## 进展更新（用户已给新 Key）

- 新 Key `sk-phgvdlgdsxkrktswiqlcmndsprcircfbaqofemsztkqfkmgp` 已写入 `.env`（原无效 key 删除）。`scripts/verify_key.py` 验证 chat+embedding 均 200。
- **整条流水线已跑通**：`drive_dm_ingest.py --force` 第二次运行 EXIT=0。结果文档 `90e692da-...`：OCR 20 页 → 分块 22 → 去重 22 → 4 批问答对 **45 条** → 向量化入库全部成功。
- **Bug 修复 1（embedding 超长 400）**：`BAAI/bge-large-zh-v1.5` 在硅基流动上输入上限约 **512 token（实测 400 字 OK / 512 字 400）**。长 chunk（均值 669 字，最长 1200）触发 `400 code 20015`。
  - 修复：`app/core/config.py` 加 `embedding_max_chars=480`；`app/services/llm.py` 的 `_embed_payload` 对每条 input 截断到该上限（保留头部）。入库 content 仍是全文，仅向量表示覆盖前 ~480 字（匹配 bge 自身只看前 512 token 的行为）。
  - 探针脚本 `scripts/probe_embed_limit.py` 可复现阈值；`scripts/verify_key.py` 已含截断单测。
- **Bug 修复 2（检索 RPC 400，待用户执行 SQL）**：`match_dm_chunks` / `match_dm_qa` 函数体里 `execute format('set local hnsw.ef_search = ...')` 与函数声明 `stable` 冲突 → Postgres 报 `0A000 SET is not allowed in a non-volatile function`。
  - 修复：`sql/dm_rag.sql` 把这两个函数 `stable` → `volatile`（已改，create or replace 幂等）。
  - **需用户在 Supabase SQL Editor 重跑 `sql/dm_rag.sql`**（DDL 无法经 PostgREST 改，只能 dashboard 执行）。之后 `scripts/verify_retrieval.py` 即可验证召回。
- 新增脚本：`scripts/verify_key.py`、`scripts/probe_embed_limit.py`、`scripts/verify_retrieval.py`、`scripts/drive_dm_ingest.py`（保留）。

## 最终验证（用户已重跑 SQL）

- `scripts/verify_retrieval.py` 重跑 EXIT=0，**RPC `0A000` 已消除**，端到端 RAG 可用。
- 召回结果（hybrid，top_k=3）：
  - 「萧何七个人格」→ qa 命中 sim=0.8589/0.8574（极准），chunk 偏弱（0.605 版权页）。
  - 「许彤怎么死」→ chunk P12-13（绑架杀人案死者许彤）sim=0.5509（准）。
  - 「搜证次数/星期一轮到话术」→ chunk/qa 命中 sim 在 0.3~0.45 区间（偏弱）。
- **召回质量观察**：qa 表（45 条）召回普遍优于 chunk，因 qa 是 LLM 提炼的短问答对，落在 bge 窗口内。chunk 召回偏弱源于「整页当 1 chunk（最长 1200 字）+ 截断到 480 字」——部分页关键信息在 480 字之后被截，向量只覆盖前段。
- **可选优化（未做，待用户定）**：① 分块粒度改细（每 ~400 字切一段）让每段都在 bge 窗口内；② 长 chunk embedding 改为「章节标题+正文前段」拼接到头部；③ 检索以 qa 为主召回、chunk 为补充。当前已是可用基线。
- 本地仍 `CELERY_EAGER=true`；上线 Railway 前改回 `false`。

## 检索优化：以 qa 为主召回

- 需求：检索召回优先用 qa（精准、短、落在 bge 窗口内），chunk 仅作原文佐证。
- 改动：
  - `app/core/config.py` 新增 `dm_search_qa_supplement_k=3`（hybrid 下 chunk 补充配额上限）、`dm_search_qa_boost=1.12`（qa 排序加权）。
  - `app/schemas/dm_guide.py` 新增 `RetrievedHit`（type/similarity[加权]/raw_similarity/payload），`SearchResult.hits` 字段 = qa 优先的扁平召回视图。
  - `app/services/dm_service.py` 的 `search`：hybrid 下 `qa_k=top_k`、`chunk_k=min(top_k, qa_supplement_k)`；新增 `_merge_hits_qa_first` 合并、qa 乘 boost 排前。
- 验证：`scripts/verify_retrieval.py` 改展示 qa 主/chunk 补充 + hits 顺序。重跑 4 查询均「qa 8 条 / chunk 3 条」，hits 前排 qa 主导，真实高相关 chunk 仍自然进前排。EXIT=0。
- 注意：API `dm_guides.py` 直接 `return service.search(...)`，新增 `hits` 字段向后兼容（旧字段 chunks/qa 保留）。`answer` 函数本项目尚未实现，未来若拼上下文应优先消费 `hits`。

## Railway 部署清单（已交付 `RAILWAY_DEPLOY.md`）

- 基于现有 `Dockerfile`（单镜像 API+4 worker，supervisord 编排）/`railway.json`（DOCKERFILE 构建、`/ready` 健康检查）/`supervisord.conf`（`${PORT:-8000}`）/`.dockerignore`（已排除 `.env`）与 `/api/v1/system/ready` 路由，生成可照做的上线步骤。
- 架构：Railway 托管后端（API+4 worker），RabbitMQ 用 CloudAMQP add-on，Redis 用 **Railway 自带 Redis**（长连接友好，优于 Upstash serverless）。Supabase/OSS/OCR/SiliconFlow 均为远程服务，注入密钥即可。
- 环境变量重点（已在清单列成表）：①`CELERY_EAGER` 必须改 `false`（本地是 true 免 broker，上线前在 Railway 变量设 false 即可，不必改本地文件）；②`CELERY_BROKER_URL`/`CELERY_RESULT_BACKEND`/`CELERY_REDIS_URL` 指向 add-on，禁止 `127.0.0.1`；③`CORS_ORIGINS` 加 Vercel 前端域名；④OSS/Supabase/SiliconFlow/OCR 直接复制本地值。
- 已列 6 个常见坑（CloudAMQP 免费连接数限制、EAGER 忘改 false、broker 写 localhost、CORS 漏配、镜像误带 .env、换 embed 模型不同步改 SQL 的 vector(N)）。
