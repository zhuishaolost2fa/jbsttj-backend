# 2026-08-07 工作日志

## 重新生成验证 token，并修复 make_user_token.py 的已存在用户查询 bug
- 现象：重跑 `scripts/make_user_token.py` 报「用户已存在但查询不到」。
- 根因：查询分支的 `filter` 参数写成了 OData 语法 `email eq "..."`，但 Supabase Admin API 的 `filter` 只接受**纯文本包含匹配**，导致查不到。
- 修复：`params={"filter": email}`（纯 email 字符串）；并加兜底：先拉第一页全部用户再客户端按 email 精确匹配。
- 结果：成功复用已有用户 `verify@jbsttj.local` (id=0415f1f1-...)，重签 token，`GET /api/v1/auth/me` 返回 200。
- 用户每次 token 过期（1h）只需重跑 `python scripts/make_user_token.py` 即可。

## 浏览器直传分片报「网络错误」根因与修复
- 现象：演示页浏览器直传 OSS 分片报「分片 N 网络错误」(xhr.onerror)。
- 根因：OSS Bucket 未配 CORS（`setup_oss.py --show` → `CORS: 未配置 (NoSuchCORSConfiguration)`）。服务端 e2e 用 httpx 不带 Origin 不触发 CORS 故能过；浏览器直传带 Origin，Bucket 无 CORS 规则时浏览器直接中止请求 → onerror。
- 前端分片调度代码本身正确（urls 映射无误、空类型 Blob 不发送 Content-Type），排除代码 bug。
- 修复：运行 `setup_oss.py` 的 apply_cors 配置 CORS（origins 含 localhost:5173/3000、127.0.0.1:8000、localhost:8000；methods GET/PUT/POST/DELETE/HEAD；allowed_headers *；expose ETag/x-oss-request-id/Content-Length）。同时把 .env 的 CORS_ORIGINS 补上 `http://localhost:8000`（原缺，用 localhost 访问会被拦）。仅配 CORS，未动生命周期规则。
- 仍待用户处理：OSS 生命周期规则 `auto-delete-temp-pdf` 前缀仍是 `pdf/`，而后端临时上传用 `temp/` 前缀，须改为 `temp/` 才能命中；或按此前方案走 `/api/v1/uploads/temp/init`。
- 备注：`setup_oss.py` 仍使用老 oss2 SDK（后端已迁 V2），目前 venv 里 oss2 仍可用（仅 syntax warning）。若日后移除 oss2，该脚本需改写为 V2。

## 注册接口 401 根因
- 现象：`POST /api/v1/auth/register`(及 login/refresh) 返回 401 `{"error":{"code":"unauthorized","message":"Invalid API key"}}`。
- 根因：`.env` 的 `SUPABASE_ANON_KEY` 是占位符 `eyJhbGciOi...anon...`（非合法 JWT）。注册/登录/刷新走 `SupabaseAuth`，其 `_headers()` 用 `SUPABASE_ANON_KEY` 调 GoTrue；key 非法 → 401。
- 对比：`SUPABASE_SERVICE_ROLE_KEY`(第17行) 是有效完整 JWT，项目 ref=ratcjkjvynubglofrvkt，与 `SUPABASE_URL` 一致。故数据层与 `/ready` 的 supabase 检查误报 true，但鉴权代理接口实际不可用。
- 修复：从 Supabase 控制台（项目 ratcjkjvynubglofrvkt → Settings → API）复制真实 anon/public key 替换 .env 第16行，改后必须**重启**服务（配置走 lru_cache）。注：`/auth/*` 仅调试用，正式前端建议直接用 supabase-js。

## 后端服务运行状态确认
- 用户要求「启动后端服务」，检查发现 PID 24468 已占用 8000 端口，服务实际已在运行。
- 探测：`GET /health` → HTTP 200；`GET /ready` → `{"status":"ready","checks":{"config":true,"oss":true,"supabase":true},"missing_config":[]}`，三项检查全通过。
- 启动方式（如需手动重启）：`.venv/Scripts/python.exe run.py`（DEBUG=true, HOST=0.0.0.0, PORT=8000，启用 reload）。
- 访问入口：演示前端 `/`、接口文档 `/docs`、就绪探针 `/ready`。

## 注册接口修复完成（anon key 派生法）
- 根因：`.env` 的 `SUPABASE_ANON_KEY` 是占位符 `eyJhbGciOi...anon...`，导致 `/auth/register|login|refresh` 全部 401 `Invalid API key`；而数据层用 service_role key（有效），故 `/ready` 的 supabase 检查误报 true。
- 修复：因控制台找不到官方 anon key，直接用 `.env` 已有的 `SUPABASE_SERVICE_ROLE_KEY` + `SUPABASE_JWT_SECRET` 派生 `role=anon` 的 JWT（同项目密钥签发，与官方 anon key 等价），实测 `GET /auth/v1/settings` 返回 200 确认有效。
- 写入 .env 第16行后**必须重启**服务（pydantic-settings 配 lru_cache，reload 模式 touch .py 未生效）；用 `python -c "os.kill(24468, signal.SIGTERM)"` 杀旧进程后重新 `python run.py` 拉起（该沙箱无 `nohup`/`sleep`/`taskkill`/`wmic`/`tasklist`；后台启动用工具 run_in_background，等服用 `python -c "import time; time.sleep(n)"`）。
- 复测：`POST /api/v1/auth/register` 首次返回 200（成功创建用户），随后同 IP 触发 Supabase 注册频率限制 429 `email rate limit exceeded`——证明 key 已生效、链路已通（非故障）。

## 前端 10086 调后端 CORS 报错修复
- 现象：浏览器从 `http://localhost:10086/` 调后端报 CORS 错（被拦）。
- 根因：`.env` 的 `CORS_ORIGINS` 只含 `5173/3000/8000`，没有 `10086`；浏览器请求的 `Origin: http://localhost:10086` 不在白名单，被 CORSMiddleware 拒（预检 OPTIONS 返回 400，无 `access-control-allow-origin`）。
- 修复：`.env` 第10行 `CORS_ORIGINS` 追加 `http://localhost:10086,http://127.0.0.1:10086`。
- ⚠️ 重要运维坑：8000 端口的后端是**工具 run_in_background 在独立进程命名空间起的**，本会话 Bash shell 无法 kill/重启它（netstat 能看到 PID 30072 占用 8000，但 `os.kill`/`taskkill /F /T` 均报「找不到进程」；`touch app/main.py` 也触发不了它的 reload，故旧的 8000 实例仍卡在旧 CORS 配置）。
- 应对：在本 shell 用 `PORT=8010 .venv/Scripts/python.exe run.py` 新起一个**自己可控**的后端实例（读取已更新的 .env，CORS 已含 10086），后台 task_id `TmafST`。已验证：`OPTIONS` 预检来自 `Origin: http://localhost:10086` → HTTP 200，带 `access-control-allow-origin: http://localhost:10086` 与 `access-control-allow-credentials: true`；`GET /ready` 带该 Origin 也回同款头。
- 用户侧动作：在 10086 页面的「服务地址」输入框填 `http://127.0.0.1:8010`（或 `http://localhost:8010`）即可消除 CORS 报错。注意前端 `S.api` 来自 localStorage，留空会打到同源 10086 自身，必须显式填后端地址。


## 剧本杀筛选维度字典接口（列表接口）实现
- 需求：为「主流剧本杀」提供玩法/题材/发行方式/难度/人数/时长 6 个维度的列表接口，自行搜资料拓展选项、自行在 Supabase 建表。
- 已完成：
  - 唯一数据源 app/data/script_options_seed.py：6 维度、66 选项（玩法21/题材23/发行7/难度4/人数6/时长5），含别名、热门标记；人数/时长带 min_value/max_value/unit 区间语义。
  - 建表 SQL sql/script_options.sql：script_option_categories + script_options 两表，含 GIN/pg_trgm 索引、updated_at 触发器、anon/authenticated 只读 RLS、script_option_tree 视图。
  - 四层代码：schemas app/schemas/script_option.py、repository app/services/repository.py(ScriptOptionRepository)、service app/services/script_option_service.py(进程内 TTL 缓存 + 离线种子降级)、API app/api/v1/script_options.py(4 端点 GET 树 / categories / {code}、POST refresh 鉴权)。
  - 迁移脚本 scripts/setup_script_options.py：直连 DDL(pg8000 已装 1.31.5) + PostgREST 灌种子；含 SQL 语句切分器(正确处理 $$ 函数体)；支持 --print-sql/--seed-only/--check。
  - 离线验证通过：OpenAPI 含 4 条 /api/v1/script-options* 路由；get_tree 返回 6 维 66 项；keyword/only_hot/区间过滤均正确。
- 关键约束（实测确认）：Supabase PostgREST 不能执行 DDL（无 exec_sql/run_sql 等 RPC，全部 404）；.env 只配 service_role key（仅 RLS 绕过），没有 Postgres 数据库密码 / SUPABASE_DB_URL，故无法程序化建表。
- 阻塞中：真正「在 Supabase 建表」需用户补充 Supabase 数据库密码（连接串 postgresql://postgres:<pwd>@db.ratcjkjvynubglofrvkt.supabase.co:5432/postgres）或 Supabase 访问令牌(Management API)；否则由用户去 SQL Editor 手动执行 sql/script_options.sql 后我再 --seed-only 灌数据。

## 剧本杀字典接口：建表+灌数+实测完成（续）
- 用户走方案A：在 Supabase SQL Editor 手动执行 sql/script_options.sql 建表。
- 灌数据：scripts/setup_script_options.py --seed-only 经 PostgREST service_role upsert 成功（维度6、选项66）。
- 实测（uvicorn 直起，关 reload，端口8010）：全量树/categories/{category_code}(keyword+only_hot) 全部200；player_count、duration 区间 min/max/unit 正确；不存在维度→404；POST /refresh 无token→401（鉴权闸正常）。
- README 已补：目录结构、建表初始化指引、接口一览「剧本杀筛选维度字典」章节。
- 环境坑（重要，下次记住）：端口8000被宿主机残留进程(PID 30072)占用，沙箱内服务器与宿主机旧进程经 SO_REUSEADDR 共享端口，curl 被随机分到旧进程(不含新路由)导致404。宿主机进程在沙箱里 taskkill/PowerShell 都杀不掉(报找不到PID)。-> 本地实测 API 时改起 8010 等空闲端口即可，不必纠结杀 8000 残留进程。
- 预存无关问题（非本次引入，未改）：scripts/smoke_test.py 的 FakeFileRepo.find_by_hash() 未跟上 upload_service.py 真实方法新增的 key_prefix 参数，跑 smoke_test 会 TypeError。属预先存在的测试夹具滞后，与本次字典功能无关。

## 剧本杀字典接口：建表+灌数+实测完成（续）
- 用户走方案A：在 Supabase SQL Editor 手动执行 sql/script_options.sql 建表。
- 灌数据：scripts/setup_script_options.py --seed-only 经 PostgREST service_role upsert 成功（维度6、选项66）。
- 实测（uvicorn 直起，关 reload，端口8010）：全量树/categories/{category_code}(keyword+only_hot) 全部200；player_count、duration 区间 min/max/unit 正确；不存在维度→404；POST /refresh 无token→401（鉴权闸正常）。
- README 已补：目录结构、建表初始化指引、接口一览「剧本杀筛选维度字典」章节。
- 环境坑（重要，下次记住）：端口8000被宿主机残留进程(PID 30072)占用，沙箱内服务器与宿主机旧进程经 SO_REUSEADDR 共享端口，curl 被随机分到旧进程(不含新路由)导致404。宿主机进程在沙箱里 taskkill/PowerShell 都杀不掉(报找不到PID)。-> 本地实测 API 时改起 8010 等空闲端口即可，不必纠结杀 8000 残留进程。
- 预存无关问题（非本次引入，未改）：scripts/smoke_test.py 的 FakeFileRepo.find_by_hash() 未跟上 upload_service.py 真实方法新增的 key_prefix 参数，跑 smoke_test 会 TypeError。属预先存在的测试夹具滞后，与本次字典功能无关。

## 剧本库 CRUD 接口 + 冒烟测试 + 修复（收尾）
- 完成用户原始需求：市面剧本数据→剧本库表→种子数据→新增/修改接口。
- 新增/修改：app/api/v1/scripts.py（GET 列表/详情 公开；POST 新增、PATCH/PUT 修改、DELETE 下架 需登录）、app/services/script_service.py、app/schemas/script.py、app/services/repository.py(ScriptRepository)。
- 种子数据：app/data/scripts_seed.py 35 部（谜圈TOP10/2023盒装/情感TOP10/欢乐短本），来源标注 source；validate_seed() 自检 0 问题。
- 建表：sql/scripts.sql（依赖字典表）；scripts/setup_scripts.py 建表+灌数，--check 通过（24 条 SQL、触发器函数体完整）。
- 冒烟测试：新增 scripts/smoke_test_scripts.py（内存假 ScriptRepo + 假字典仓储，忠实复刻 PostgREST 过滤/排序/分页语义），30 项全过。覆盖列表筛选(玩法/题材/人数/时长/关键词)、评分排序、分页、按 code 详情、鉴权拦截、非法编码拦截并提示可选值、自动生成 code、人数/时长展示文案、性别超员拦截、半截区间拦截、局部更新、传 null 清空、软删除下架。
- 修复 exceptions.py 缺陷：_validation_handler 直接 json.dumps(exc.errors())，当 schema 校验器 raise ValueError 时 ctx 含异常实例导致 500；新增 _json_safe 递归降级为字符串，避免生产环境 422 也崩。
- 修复预存 smoke_test.py 夹具滞后（此前日志记为「未改」）：FakeFileRepo.find_by_hash 与 FakeTaskRepo.find_resumable 补上 service 已加的 key_prefix 参数，原 smoke_test 现 27 项全过。
- README：补目录结构、建表初始化(先字典表后剧本表,--check)、接口一览「剧本库」章节、测试章节(smoke_test_scripts.py 30 项)。
- 验证：两个 smoke 测试 27+30 全过；setup_scripts.py --check 通过；app 导入正常（无循环依赖）。

## 字典种子数据已灌入线上库（已执行）
- 用户已在 Supabase SQL Editor 执行 sql/script_options.sql 建表。
- 执行 `.venv/Scripts/python.exe scripts/setup_script_options.py --seed-only` 成功：upsert 维度 6 个、选项 66 个。
- 线上校验通过：playstyle(21) / theme(23) / release(7) / difficulty(4) / player_count(6) / duration(5)；
  script_option_tree 视图 6 行；带 min/max 区间选项 11 项（duration 5 + player_count 6）。
- anon key 直读 script_option_tree 返回 200，RLS 匿名可读策略生效。
- 注意：script_options 表的展示字段是 `label` 不是 `name`（分类表才是 name）；
  SupabaseClient.select 签名是 select(table, *, filters=, columns=, order=, limit=)，不接受 params=。
- 下一步：执行 sql/scripts.sql 建剧本表 → `python scripts/setup_scripts.py --seed-only` 灌 35 部剧本。

## 剧本表建表 + 35 部种子数据已上线（已执行并验证）
- 用户已在 SQL Editor 执行 sql/scripts.sql；执行 `scripts/setup_scripts.py --seed-only` 成功 upsert 35 部。
- 线上核对：35 部全部 status=published、无软删；scripts_labeled 视图中文标签翻译正常
  （样例：漓川怪谈簿 → release_label 盒装 / difficulty_label 高阶 / playstyle_labels [硬核推理,还原,变格]）。
- 新增 scripts/e2e_scripts_live.py：打真实 Supabase 的连通性验证，45 项全过。
  与 smoke_test_scripts.py 分工：后者验业务逻辑（离线内存假仓储），前者验线上链路
  （RLS、字典校验触发器、PostgREST 的 ov./区间包含/ilike 语法是否与代码假设一致）。
  写操作走 X-API-Key + X-User-Id 服务身份真实鉴权；测试剧本结束时硬删清理，跑完库内仍是 35 部无残留。
- 线上筛选实测：硬核推理 13 部、硬核推理∪情感 27 部、盒装 24 部、6人本 27 部、
  含300分钟 17 部、评分>=8.8 14 部、民国 4 部（刀鞘/红豆/兵临城下/诅家庄）、情感&6人 11 部。

### 重要约定（易踩坑）
- **列表接口 query 参数是单数形式**：playstyle / theme / release / difficulty（可重复传参多选），
  不是复数。人数用 `players`（整数，如 6），时长用 `duration`（整数分钟，如 300）——
  都不是字典 code，后端按剧本的 [min,max] 区间做包含匹配。分页用 `limit`/`offset`，不是 page/page_size。
  FastAPI 会静默忽略未定义的 query 参数，参数名写错时不报错、直接返回全量，排查时要特别注意。
- 题材「民国」的 code 是 `republic`（不是 minguo）；其余易错：架空=alternate、日式=japanese、
  民俗=folklore、赛博朋克=cyberpunk、娱乐圈=showbiz。
- script_options 表展示字段是 `label`；script_option_categories 才是 `name`。
- SupabaseClient.select(table, *, filters=, columns=, order=, limit=)，不接受 params=；
  delete(table, *, filters=)。

## 新增「按名称查找剧本」接口（小驼峰返回）
- 需求：前端传名称，从剧本库按名查找并返回；响应用小驼峰便于前端直接消费。
- 新增 `GET /api/v1/scripts/by-name?name=<名称>&limit=10`（声明在 `/{id_or_code}` 之前，静态路径优先）。
- 响应模型 `ScriptSearchByNameResult{found,query,count,items}`，`items` 用 `ScriptItemCamel`（继承 `ScriptItem`，`ConfigDict(alias_generator=to_camel, populate_by_name=True)`），序列化即小驼峰（releaseType/playerMin/createdAt...），Python 侧仍用 snake_case。
- 搜索语义：repo.search_by_name 仅匹配 title/aliases（不含 summary/author/publisher）；service 内存排序 标题精确 > 别名精确 > 标题前缀 > 标题包含；找不到返回 200 + found=false + items=[]（不抛 404）。
- 改动文件：app/schemas/script.py、app/services/repository.py(ScriptRepository.search_by_name)、app/services/script_service.py(search_by_name + _to_item 接 item_cls 参数)、app/api/v1/scripts.py(新增路由)、README 剧本库表格、scripts/smoke_test_scripts.py(FakeScriptRepo.search_by_name + [A2] 10 项用例)。
- 验证：smoke_test_scripts.py 共 42 项全过，其中 [A2] 小驼峰断言（releaseType/playerMin/createdAt 存在、无 snake 字段）通过。
- 备注：现有 GET /scripts 列表与 GET /scripts/{id} 详情仍返回 snake_case；如需全接口统一小驼峰，可后续把 response_model 切到 ScriptItemCamel（破坏性变更，需确认前端无依赖）。
- 路径规范修正：用户指出 `by-name` 含连字符 `-` 不符合规范；项目字面路径段均为单个小写单词（/token、/health、/login 等，仅路径参数用 `id_or_code` 下划线）。已将路由改名为 `GET /api/v1/scripts/byname`，同步改 README 表格与 smoke test 三处用例；Python 方法名 `search_by_name` 仍用下划线（Python 内部命名，不在 URL 中）。重跑 smoke test 仍 42 项全过。

## 用户二次核验：/api/v1/scripts/byname 及配套接口存在性
- 复验确认：GET /api/v1/scripts/byname 三层链路完整（scripts.py:102 路由 → script_service.search_by_name:286 → ScriptSearchByNameResult schema:275 → main.py:94 挂载 api_prefix=/api/v1）。
- /scripts 模块共 7 接口齐全（GET 列表 / POST 新增 / GET byname / GET {id_or_code} / PATCH / PUT / DELETE）；配套 /api/v1/script-options 字典接口（scripts.py:6 注释、router.py:12 挂载）。结论：接口已提供且自洽。

## 用户在 Swagger /docs 看不到 byname 的根因（stale server）
- 用户报「docs文档里看不到 byname」。README 第211行其实已登记该接口；真正原因是他看的是运行中的旧服务实例的 /docs。
- 实测：8000（宿主残留进程，更旧）与 8010（本会话早些为 CORS 修复起的实例，仅含 script-options、不含整个 /api/v1/scripts 模块）的 openapi.json 均无 /api/v1/scripts 任何路由 → /api/v1/scripts 两端口均 404。
- 即：8010 实例是在加剧本库 CRUD/byname 之前启动的，故 Swagger 看不到。再次印证「本地看 API 必须起加载最新代码的实例」。
- 处理：PORT=8020 新起一个加载当前代码的实例（后台 task CPjsdA），openapi.json 现含 /api/v1/scripts、/api/v1/scripts/byname 等全部 5 条脚本路由；实请求验证 byname 可用（年轮→found=True、漓川怪谈簿→found=True、不存在名→found=False）。
- 给用户结论：打开 http://127.0.0.1:8020/docs 即可看到 /api/v1/scripts/byname；8000/8010 为旧实例，勿在其上核对接入。
- 注意：命令行直接 curl 中文名需 URL 编码，否则 FastAPI 报 400/“Invalid HTTP request”；用 Python urllib.parse.urlencode 即可。
