# 2026-08-19

- 将生产前端域名 `https://www.jbs-ttj.store` 加入后端 CORS 白名单，改动三处：`app/core/config.py` 默认值、`.env`、`.env.local`（本地联调覆盖，优先级最高）。已验证 `cors_origin_list` 加载正常。
- 注意：后端跑在 Railway，若 Railway 环境变量中配置了 `CORS_ORIGINS`，需同步补充该域名并重新部署，否则以环境变量为准、新域名不生效。
